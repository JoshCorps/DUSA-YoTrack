let ObjectID = require('mongojs').ObjectID;
let instadate = require('instadate');
var chai = require('chai');
var expect = chai.expect;
var Month = require('../models/month.js');

describe('MonthModule', () => {
/*
    it('getColorsForDays() should return an array of colors same size as input number of days', (done) => {
        let days = {10, 20, 30, 40, 50};
        Month.getColorsForDays(days, (err, data, colors) => {
            if (err) return;
            expect(data.length).to.equal(colors.length);
            done();
        });
    });
    
    it('getColorsForDays() should return #31cc2b if revenue is more than 70% of the total revenue for the month', (done) => {
        let days = [5, 10, 20, 30, 40];
        Month.getColorsForDays(days, (err, data, colors) => {
            if (err) return;
            expect(colors[2]).to.equal("#31cc2b");
            done();
        });
    });
    
    it('getColorsForDays() should return #cc2b2b if revenue is less than 30% of the total revenue for the month', (done) => {
        let days = [5, 10, 20, 30, 40];
        Month.getColorsForDays(days, (err, data, colors) => {
            if (err) return;
            expect(colors[0]).to.equal("#cc2b2b");
            done();
        });
    });*/
  
});